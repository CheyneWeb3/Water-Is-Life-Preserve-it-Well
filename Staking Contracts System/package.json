{
  "name": "water-dividend-preserving-staking-v2-2-daily-epoch",
  "version": "2.2.0",
  "private": true,
  "description": "WATER dividend-preserving staking: Flexible + 30-day Locked pools with daily aggregate expiry epochs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:bsc-testnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "deploy:bsc": "hardhat run scripts/deploy.js --network bsc",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "2.0.7",
    "@nomicfoundation/hardhat-ethers": "3.0.8",
    "@openzeppelin/contracts": "4.5.0",
    "chai": "4.5.0",
    "ethers": "6.13.5",
    "hardhat": "2.22.17"
  }
}
